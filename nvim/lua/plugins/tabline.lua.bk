return {
  'crispgm/nvim-tabline',
  event = { 'VeryLazy' },
  config = function()
    require('tabline').setup({
      fnamemodify = ':t',
      modify_indicator = "*",
      no_name = 'No name',
      brackets = { '[', ']' },
      inactive_tab_max_length = 0
    })
  end,
}
