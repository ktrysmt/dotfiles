<?xml version="1.0" encoding="UTF-8"?>
<llmwiki-index generated="2026-04-06 22:50:38" total="27">
  <category name="services">
    <entity id="claude-code-config" updated="2026-04-06">Comprehensive Claude Code CLI configuration managed across multiple files. `claude/CLAUDE.md` defines agent behavior rules (evidence-first, output in Japanese, cite sources, no direct file deletion, no emojis). `settings.json` configures env vars (CLAUDE_CODE_DISABLE_AUTO_MEMORY=1, EXPERIMENTAL_AGENT_TEAMS=1), granular permissions (allow/deny lists for bash/file/MCP tools), hooks, statusline, and plugins. `rules/` directory provides enforcement rules for markdown formatting and PDF MCP tool selection. Statusline shows model/context/cost and rate limits.</entity>
    <entity id="homebrew" updated="2026-04-06">Package manager for macOS and Linux managing system dependencies via Brewfile manifests. Three Brewfile variants: common (cross-platform formulae), mac (macOS-specific casks), and ubuntu (Linux formulae). Installation orchestrated by `install/brew.sh`. PATH configuration handled in `.zshenv` with Apple Silicon and Intel detection.</entity>
    <entity id="mcp-servers" updated="2026-04-06">Model Context Protocol (MCP) server registry configured in `.mcp.json`. Includes both HTTP-based services (aws-docs, deepwiki, grep-github) and local command-based servers (arxiv, playwright, pdf-mcp, pdf-docling, gemini-cli, mcp-think-as, serena, o3-search, context7). Servers provide specialized capabilities for documentation lookup, code search, PDF processing, browser automation, and AI reasoning.</entity>
    <entity id="mise-runtime" updated="2026-04-06">Runtime version manager handling language runtimes, CLI tools, and development utilities. Replaces asdf. Global config at `mise/config.toml` with platform overlays for macOS (`mac.toml`) and WSL (`wsl.toml`). Manages Go, Node.js, Python (via uv), fzf, ripgrep, bat, eza, delta, jq, gh, ghq, peco, yazi, bun, buf, ruff, sheldon, and aqua-based packages. Shims activated in `.zshenv`.</entity>
  </category>
  <category name="environments">
    <entity id="devcontainer" updated="2026-04-06">Docker-based isolated development environment (sandbox) for Claude Code. Multi-architecture (amd64, arm64) image built via GitHub Actions and published to GHCR. Features eBPF-based TCP network tracing, iptables-based firewall with whitelist domain filtering, and Claude Code setup with MCP servers. Persists config via bind mounts to ~/.claude-devcontainer.</entity>
    <entity id="macos-apps" updated="2026-04-06">macOS-specific application configurations. Karabiner-Elements for keyboard remapping (kana-&gt;Enter, eisu-&gt;Backspace, Apple keyboard cmd/opt swap, Naginata IME integration). Rectangle for window tiling. iTerm2 as terminal emulator with custom keymaps and Japanese input. LinearMouse for disabling mouse acceleration. Raycast as launcher with custom script commands (browser-debug, translation). WezTerm as GPU-accelerated terminal with Lua config. MacGesture for trackpad gestures.</entity>
    <entity id="windows-apps" updated="2026-04-06">Windows-specific application configurations. Windows Terminal with 30+ custom keybindings and PowerShell/Pwsh default profile. Keyhac (Python-based key remapper) providing Ctrl+HJKL vim-like navigation with per-app overrides for Windows Terminal, Brave, and MPC-HC. WezTerm GPU-accelerated terminal (Lua config). XMBC for mouse button customization.</entity>
  </category>
  <category name="components">
    <entity id="bin-scripts" updated="2026-04-06">Collection of custom utility scripts in `bin/` directory covering git helpers, tmux integration, Kubernetes info display, and miscellaneous tools. Git helpers provide branch management, prompt info, and user display. Tmux helpers format pane borders and send keys. Kubernetes helpers display context and namespace info for prompts/status lines.</entity>
    <entity id="chrome-extensions" updated="2026-04-06">Browser extension configurations for Chrome/Chromium. Vimium provides keyboard-driven navigation with custom exclusion rules for web apps. uBlock Origin manages ad blocking and site filtering. Stylus injects custom CSS. uBlacklist filters search results. SiteBlock manages site access restrictions.</entity>
    <entity id="claude-code-hooks" updated="2026-04-06">Hook system for Claude Code lifecycle events executing bash/python scripts. Hooks trigger on SessionStart, SessionEnd, PostToolUse, UserPromptSubmit, Notification, and Stop events. Key hooks: cleanup-short-sessions (delete transcripts with &lt;2 user messages), session-summarizer (AI-generated session summaries), markdown-lint (enforce formatting rules), sandbox-auto-allow (auto-detect and whitelist blocked domains), and tmux-window-claude-status (update tmux window state).</entity>
    <entity id="fzf-integration" updated="2026-04-06">FZF is integrated across shell (zsh) and editor (Neovim) environments for fuzzy searching. In the shell, FZF_DEFAULT_COMMAND uses `fd` with hidden files, FZF_DEFAULT_OPTS uses bat for preview. Custom keybindings include Ctrl-T (file preview) and Ctrl-V (snippet insertion via gss). In Neovim, fzf-vim provides Files, GFiles, Buffers, Commands, Ripgrep, History, and Windows commands. Peco is also configured as a lighter alternative fuzzy filter.</entity>
    <entity id="git-config" updated="2026-04-06">Platform-specific git configurations for macOS, Ubuntu, and WSL. All variants use SSH signing (id_ed25519.pub), delta for diff viewing (Dracula theme), ghq for multi-repository management (roots: ~/go/src, ~/project/src), and git-secrets for preventing credential leaks. Global gitignore patterns cover .claude/, __pycache__, node_modules, and other common exclusions.</entity>
    <entity id="neovim-editor" updated="2026-04-06">Neovim configuration using lazy.nvim as plugin manager. Entry point is `init.lua` which requires options, keys, lazynvim, and highlight modules. Symlinked to `~/.config/nvim`. Plugin files follow naming convention: `ft.*` (filetype), `appearance.*` (UI), `move.*` (motion), `llm.*` (AI). Local machine-specific configuration is handled via `local.nvim` plugin providing autocommands, custom commands, clipboard integration, and health checks.</entity>
    <entity id="nvim-lsp-completion" updated="2026-04-06">Language Server Protocol (LSP), Tree-sitter, and autocompletion configuration. nvim-lspconfig manages 13 language servers via mason-lspconfig. Tree-sitter provides incremental parsing with textobjects, textsubjects, and matchup support for 20+ languages. nvim-cmp is the completion engine with sources from LSP, snippets (vsnip), buffer, path, signature help, and calculator.</entity>
    <entity id="nvim-plugins-ai" updated="2026-04-06">AI-powered code assistance in Neovim. Active plugins: Claude Code integration (claudecode.nvim) and GitHub Copilot. Disabled plugins: Avante, Gemini, and Sourcegraph Cody. Claude Code runs via Coder with port range 10000-65535, supporting fallback to Ollama or OpenRouter. Copilot provides auto-triggered suggestions with S-Tab to accept.</entity>
    <entity id="nvim-plugins-editing" updated="2026-04-06">Editing enhancement plugins covering formatting, linting, commenting, smart bracket insertion, text alignment, sorting, undo management, and yank ring. format-on-save uses prettier for JS/TS and LSP for other languages. nvim-lint integrates eslint. Lexima provides smart brackets with a LeximaAlterCommand system for command aliases.</entity>
    <entity id="nvim-plugins-ft" updated="2026-04-06">Extended language and filetype support for development workflows. Active plugins provide syntax and tooling for Ansible YAML, CSV/TSV, Astro templates, Svelte, Terraform HCL, and quickfix manipulation. Disabled plugins include better quickfix (nvim-bqf) and Go struct tag generation (vim-goaddtags).</entity>
    <entity id="nvim-plugins-git" updated="2026-04-06">Git integration in Neovim combining vim-fugitive, gitsigns, git-conflict, diffview, tig-explorer, and octo (GitHub). Gitsigns shows diff signs with hunk navigation (gw/gb) and staging (g+/g-). Git-conflict handles merge conflict resolution. Diffview provides visual diff for commits and file history. Tig-explorer integrates the terminal git log browser. Octo enables GitHub PR review and issue management within Neovim.</entity>
    <entity id="nvim-plugins-motion" updated="2026-04-06">Motion and text object plugins enabling fast navigation and smart text selection. EasyMotion provides label-based character jumping (leader: semicolon). CamelCase motion treats camelCase as multiple words. Edge motion jumps to line boundaries. Treesitter-unit enables syntax-aware text objects. Vim-expand-region provides incremental selection. Additional plugins: vim-asterisk (search stay-in-place), vim-abolish/vim-camelsnek (case coercion), plus bundled leap, treehopper, vim-sandwich, and vim-niceblock.</entity>
    <entity id="nvim-plugins-ui" updated="2026-04-06">Visual and UI enhancement plugins for Neovim. Lualine provides a customized status line with gruvbox-material theme showing branch, diff, diagnostics, filename with path, and search count. Neo-tree is the file tree explorer with git status integration. Yazi.nvim integrates the yazi terminal file manager with floating window support. Outline provides a sidebar for code structure via treesitter/LSP/ctags. Additional UI plugins handle code chunk highlighting, scrollbar with search results, fold visualization, and auto indent detection. Pinecone is the custom color scheme.</entity>
    <entity id="sheldon-plugins" updated="2026-04-06">Sheldon is the zsh plugin manager used to orchestrate plugin loading. Configuration is defined in `sheldon.plugins.toml` (symlinked to `~/.config/sheldon/plugins.toml`). Uses a defer-loading template powered by `zsh-defer` for optimized startup. Manages both external GitHub plugins and local dotfiles modules.</entity>
    <entity id="tmux-config" updated="2026-04-06">Platform-specific tmux configurations for macOS, Ubuntu, WSL, and Vagrant. Prefix key is Ctrl+S. Uses vi-copy mode, custom pane border with git branch and directory info, and status line with timestamp. History limit is 10 million lines. Pane border info rendered by `bin/tmux-pane-border` script. `bin/tsk` provides a convenience wrapper for `tmux send-keys`. Auto-rename function in zsh updates window name based on current directory.</entity>
    <entity id="zsh-shell" updated="2026-04-06">Zsh shell configuration system split into synchronous (blocking) and asynchronous (deferred) phases for optimized startup. `.zshenv` handles earliest PATH/environment setup (Homebrew, mise shims, TMUX socket), while `.zshrc` bootstraps sheldon plugin cache. `sync.zsh` configures history options, completions, and shell options. `async.zsh` handles all functions, aliases, keybindings, and heavy logic via deferred loading. Platform-specific credential helpers exist for WSL (gcm.zsh) and macOS (osx.zsh).</entity>
  </category>
  <category name="procedures">
    <entity id="claude-code-skills" updated="2026-04-06">Custom skill templates for Claude Code providing specialized workflows. Two categories: browser automation (agent-browser standalone and CDP mode) and Agent Teams workflows (fullstack feature development, parallel code review, parallel research, parallel refactoring, competing hypothesis debug). Agent Teams skills use TeamCreate to spawn parallel agents with distinct identities and file ownership.</entity>
    <entity id="dotfiles-install" updated="2026-04-06">Idempotent installation system orchestrated by `install/update.sh`. Execution chain: symlink.sh -&gt; brew.sh -&gt; mise.sh -&gt; OS-specific scripts -&gt; post-install.sh -&gt; sheldon cache rebuild. `lib.sh` provides shared helpers (link_file, ensure_dir, log_*) with automatic OS detection (mac/wsl/vagrant/ubuntu via uname). All scripts are safe to run multiple times without side effects.</entity>
  </category>
  <category name="concepts">
    <entity id="agent-teams" updated="2026-04-06">Team-based parallel execution pattern using Claude Code's TeamCreate API. A Lead agent coordinates multiple Teammates with distinct identities, file ownership, and phases. Teammates can be Read-Only (investigation/review) or Read-Write (implementation/refactoring). Key principles: exclusive file ownership prevents conflicts, phased execution manages dependencies, and team CLAUDE.md annotations specify boundaries.</entity>
    <entity id="credential-management" updated="2026-04-06">Three-tier credential management system adapted per platform. WSL2 uses Git Credential Manager (gcm.zsh: gcm-get/set/rm/ls). macOS uses Keychain (osx.zsh: kc-get/set/rm/ls). Bitwarden CLI (btw.zsh_bk, currently disabled) provides a universal fallback. All systems manage the same set of keys defined in `.keys.example` (ANTHROPIC_API_KEY, GEMINI_API_KEY). Credentials are exported as environment variables and used by shell functions and external tools.</entity>
  </category>
</llmwiki-index>
